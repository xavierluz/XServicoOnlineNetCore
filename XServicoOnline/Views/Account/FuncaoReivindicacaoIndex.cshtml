@model XServicoOnline.Models.FuncaoReivindicacao
<div class="wrapper">

    <div class="main-panel data-grid">
        <div class="content pagina-index">
            <div class="container-fluid alinhamento-grid-index">
                <div class="row">

                    <div class="col-md-12">
                        <a asp-controller="Account" asp-action="CreateFuncaoReivindicacao" class="btn btn-primary btn-round">Novo</a>
                        <div class="card">
                            <div class="card-header card-header-primary">
                                <h4 class="card-title ">Reinvidicação</h4>
                                <p class="card-category">Reinvidicações da função cadastradas</p>

                            </div>

                            <div class="card-body">
                                <div class="table-responsive row-grid">
                                    <table id="tableFuncaoReivindicacao" class="table table-bordered border-table">
                                        <thead>
                                            <tr>
                                                <th class="border-table-tr reivindicacao" data-sortable="true">Reinvidicação</th>
                                                <th class="border-table-tr valor" data-sortable="true">Valor</th>
                                                <th class="border-table-tr td-invisivel" data-sortable="false"></th>
                                                <th class="border-table-tr td-invisivel" data-sortable="false"></th>
                                                <th class="border-table-tr actionButton" data-sortable="false"></th>
                                            </tr>
                                        </thead>
                                        <tbody class="body-funcao-reivindicacao" data-toggle="table"></tbody>
                                    </table>
                                </div>

                            </div>

                        </div>
                        <partial name="_PrincipalPartial" />
                    </div>

                </div>

            </div>
        </div>

    </div>
</div>

<!-- /.login-box -->
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    @{await Html.RenderPartialAsync("_DataTableReferenciaJS");}
    <script src="~/assets/js/account/FuncaoReivindicacaoDataTable.js"></script>
    <script>
        $(document).ready(function () {
            var funcaoReivindicacaoDataTable = new FuncaoReivindicacaoDataTable();
            funcaoReivindicacaoDataTable.Load();
            $('.active').removeClass();
            $('.material-link').addClass('active');
            $(function () {
                $('[data-toggle="tooltip"]').tooltip()
            })
        });
    </script>

}

